{
  "runes": {
    "backup": {
      "path": ".crunes/runes/backup.js",
      "name": "Backup src/",
      "description": "Zip the src/ directory into backups/<timestamp>.zip and report its contents",
      "permissions": {
        "use": {
          "allow": ["fs.read:src/**", "fs.glob:src/**", "fs.write:backups/**"]
        }
      }
    },
    "restore": {
      "path": ".crunes/runes/restore.js",
      "name": "Restore latest backup",
      "description": "Extract the most recent backup zip into restore/",
      "permissions": {
        "use": {
          "allow": ["fs.read:backups/**", "fs.glob:backups/**", "fs.write:restore/**", "fs.glob:restore/**"]
        }
      }
    }
  }
}
