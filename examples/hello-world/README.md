# Hello World

The simplest possible crunes rune — returns a single greeting section.

## What it demonstrates

A minimal rune with no arguments, no permissions, and no side effects. The entry point is `export async function use()` returning a section created with `section.create`.

## How to run

```bash
crunes use hello
```

## What to expect

A single markdown section containing `Hello, World!`.
