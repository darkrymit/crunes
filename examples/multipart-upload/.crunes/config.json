{
  "name": "multipart-upload",
  "runes": {
    "upload": {
      "name": "Multipart File Upload",
      "description": "Uploads a local file as a multipart/form-data POST and streams the response body.",
      "permissions": {
        "use": {
          "allow": [
            "fs.read:./**",
            "http.fetch:POST::https://httpbin.org/post"
          ]
        }
      }
    }
  }
}
