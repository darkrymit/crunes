{
  "name": "spawn-demo",
  "runes": {
    "server": {
      "name": "Spawn Demo — Server",
      "description": "Manages the background worker lifecycle — start, stop, or status.",
      "permissions": {
        "run": {
          "allow": [
            "fs.write:.crunes/state/spawn-demo.json",
            "rune.job.start:worker",
            "rune.job.kill",
            "rune.job.exists"
          ]
        }
      }
    },
    "worker": {
      "name": "Spawn Demo — Worker",
      "description": "Long-running background process kept alive for up to one hour.",
      "permissions": {
        "run": {
          "allow": ["shell:**"]
        }
      }
    }
  }
}
