{
  "name": "streaming-demo",
  "runes": {
    "stream-count": {
      "name": "Streaming Demo",
      "description": "Streams subprocess output progressively, emitting section updates until a tick limit.",
      "permissions": {
        "run": {
          "allow": ["shell.exec:node counter.js"]
        }
      }
    },
    "stream-fetch": {
      "name": "Stream Fetch",
      "description": "Streams an HTTP response body chunk-by-chunk using res.body().getReader().",
      "permissions": {
        "run": {
          "allow": ["http.fetch:GET::**"]
        }
      }
    },
    "stream-shell-features": {
      "name": "Stream Shell Features",
      "description": "Demonstrates WHATWG Web standard streams, stdin stream piping, and binary mode in sandboxed shell executions.",
      "permissions": {
        "run": {
          "allow": [
            "shell.exec:node pipe.js",
            "shell.exec:node binary.js",
            "shell.exec:node counter.js"
          ]
        }
      }
    }
  }
}
