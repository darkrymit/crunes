{
  "name": "task-manager",
  "runes": {
    "init": {
      "name": "Init",
      "description": "Create tasks.json if it does not exist.",
      "permissions": {
        "use": {
          "allow": [
            "fs.read:./tasks.json",
            "fs.write:./tasks.json"
          ]
        }
      }
    },
    "task": {
      "name": "Task Manager",
      "description": "Per-project task tracker with nested tag management.",
      "permissions": {
        "use": {
          "allow": [
            "fs.read:./tasks.json",
            "fs.write:./tasks.json"
          ]
        }
      }
    }
  }
}